package com.mvc;

// import org.springframework.context.ApplicationContext;
// import org.springframework.context.annotation.AnnotationConfigApplicationContext;

// import com.mvc.Controller.BusController;
// import com.mvc.Controller.StationController;
// import com.mvc.Service.RouteService;

// import com.mvc.Entity.Student;

public class App 
{
    public static void main( String[] args )
    {
        // ApplicationContext context = new ClassPathXmlApplicationContext("context.xml");
        // ApplicationContext context = new AnnotationConfigApplicationContext(FrontController.class);

        // // StationController sc = context.getBean(StationController.class);
        // // sc.add();
        // // System.out.println("station added.");
        // BusController rs = context.getBean(BusController.class);
        // System.out.println(rs.getBus());

        // RouteService r = context.getBean(RouteService.class);
        // System.out.println(r.getRoute());

    }
}
